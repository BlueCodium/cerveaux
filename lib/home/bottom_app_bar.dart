import 'package:flutter/material.dart';
import '../var.dart';

class BottomApBar extends StatelessWidget {
  const BottomApBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const BottomAppBar(
      color: Colors.red,
    );
  }
}
